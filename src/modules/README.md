# Business modules

Feature modules will be added here in roadmap order. A module owns its server-side business actions, validation, queries, and tests; it does not bypass shared authorization, audit, idempotency, or transaction services.
